IG_ACCESS_TOKEN=
IG_BUSINESS_ID=
GITHUB_REPO_OWNER=
GITHUB_REPO_NAME=
APILEAGUE_API_KEY=
# No ANTHROPIC_API_KEY needed -- the weekly Generate routine is itself a
# scheduled Claude Code agent, so caption/hashtag/meme-text writing happens
# as part of that agent's own turn (see pipeline/captions.py).
# IMGUR_CLIENT_ID / REDDIT_CLIENT_ID / REDDIT_CLIENT_SECRET / REDDIT_USER_AGENT:
# unused, imgur_source.py and reddit_source.py are dormant (see
# docs/superpowers/specs — Repost Sourcing)
